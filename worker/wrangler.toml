name = "hsk-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "hsk-db"
database_id = "local-hsk-db"

# KV Namespace binding (for sessions/cache if needed)
[[kv_namespaces]]
binding = "CACHE"
id = "local-hsk-cache"
